- if alert.present?
  .alert.alert-error
    %button.close{'data-dismiss' => 'alert', :type => 'button'} &times;
    = alert
- if notice.present?
  .alert.alert-success
    %button.close{'data-dismiss' => 'alert', :type => 'button'} &times;
    = notice
